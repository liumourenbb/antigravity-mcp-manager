# 项目规范：代码精简与架构防膨胀规范 (Code Simplification & Anti-Bloat Architecture Standards)

为保障工程的可维护性、可测试性与架构整洁度，避免随着业务迭代出现代码腐化与“上帝类”，本项目代码开发与审查必须严格遵守以下规范：

---

## 1. 单方法 80 行红线 (80-Line Method Limit)
- **行数红线**：单个方法的总代码量严格限制在 **80 行以内**（含方法签名、空行及注释）。
- **超限禁止平铺**：代码行数超限时，严禁在原方法中直接线性平铺逻辑；必须按照单一职责原则（SRP）进行纵向或横向拆解，抽取为私有方法或外部独立组件（Helper / Manager）。

---

## 2. 杜绝制造“上帝类” (No God Objects / Classes)
- **外部调用与新链路隔离**：新增业务链路、外部 RPC 接口调用或第三方服务交互时，必须建立独立的 `Manager` 或 `Helper` 类承接，严禁堆砌在既有 `Service` 类中，防止单个业务 Service 职责无限膨胀。
- **领域模型独立化**：`VO`、`DTO`、`Query`、`BO` 等领域模型与数据传输对象，必须独立创建 `.java` 文件（及对应语言的独立模型文件），严禁在业务类中声明复杂内部类（Inner Class），确保领域模型定义清晰、边界独立、易于跨层复用。

---

## 3. 最小改动拆分——新增外置、原位路由 (Minimal Inline Changes: Externalize & Route)
- **老方法扩展原位控制**：在对老方法/既有方法进行业务扩展或重构时，原位核心改动原则上控制在 **1~3 行**，仅保留路由、分发与调度逻辑：
  - **分支解耦**：分支扩充采用策略模式（Strategy）或处理器模式（Handler）替代无限叠加的 `if-else` / `switch`；
  - **旁支外置**：繁杂的前置参数校验、业务规则检查、复杂数据转换与组装等旁支逻辑，封装为独立的 `Helper` 组件注入调用；
  - **事务与汇聚下沉**：多数据源聚合、跨模块数据汇聚以及通用事务逻辑，统一下沉至独立的 `Manager` 层实现。

---

## 4. 全局用户规则集成 (Global User Rules)
1. 保持独立思考与求真核实：严审前提漏洞与逻辑缺陷，严格区分事实、推测与观点，核实数据与结论来源，拒绝盲从迎合；
2. 直陈异议与盲点提示：有不同意见直接指出并给出依据、风险及替代方案，主动提示遗漏的变量、潜在成本与偏差；
3. 任务闭环与严格验证：任务完成后必须进行严格验证（如测试、构建或结果校验）；若未完全完成，必须生成结构化报告明确【要干什么】、【干了什么】和【还有什么没干】；
4. 数据库安全与云端备份隔离：高危变更前强制备份并隔离保存于云端硬盘，严禁保存在临时工件目录中；
5. 语言与思考偏好：内部思考分析与最终回答均尽量使用中文；
6. 服务器操作留痕与日志规范：远程命令与服务启停必须明确记录操作意图与结果，涉及密码密钥等敏感信息主动脱敏；
7. 打包确认红线：任何打包构建（包括本地编译 exe 与生成发布 zip）必须经过用户明确确认之后方可执行。
